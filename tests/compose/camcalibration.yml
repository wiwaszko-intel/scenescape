# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

networks:
  scenescape-test:

secrets:
  root-cert:
    file: ${SECRETSDIR}/certs/scenescape-ca.pem
  django:
    file: ${SECRETSDIR}/django
  calibration.auth:
    file: ${SECRETSDIR}/calibration.auth
  camcalibration-cert:
    file: ${SECRETSDIR}/certs/scenescape-camcalibration.crt
  camcalibration-key:
    file: ${SECRETSDIR}/certs/scenescape-camcalibration.key

services:
  # Init container to fix volume permissions for camcalibration
  camcalibration-init:
    image: alpine:3.19
    command: sh -c "chown -R 1000:1000 /data/netvlad_models /data/media /data/datasets"
    volumes:
      - vol-netvlad_models:/data/netvlad_models
      - vol-media:/data/media
      - vol-datasets:/data/datasets
    restart: "no"

  camcalibration:
    image: scenescape-camcalibration:${VERSION:-latest}
    init: true
    networks:
      scenescape-test:
        aliases:
          - camcalibration.scenescape.intel.com
    user: "1000:1000"
    depends_on:
      camcalibration-init:
        condition: service_completed_successfully
      web:
        condition: service_healthy
      broker:
        condition: service_started
    command: >
      --restauth /run/secrets/calibration.auth
      --resturl https://web.scenescape.intel.com:443/api/v1
      --restport 8443
      --ssl-certfile /run/secrets/certs/scenescape-camcalibration.crt
      --ssl-keyfile /run/secrets/certs/scenescape-camcalibration.key
    ports:
      - "8443:8443"
    environment:
      - EGL_PLATFORM=surfaceless
      - NETVLAD_MODEL_DIR=/usr/local/lib/python3.10/dist-packages/third_party/netvlad
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - NO_PROXY=${NO_PROXY}
    volumes:
      - ./:/workspace
      - vol-media:/home/scenescape/SceneScape/media
      - vol-sample-data:/workspace/media
      - vol-datasets:/home/scenescape/SceneScape/datasets
      - vol-netvlad_models:/usr/local/lib/python3.10/dist-packages/third_party/netvlad
    secrets:
      - source: root-cert
        target: certs/scenescape-ca.pem
      - source: camcalibration-cert
        target: certs/scenescape-camcalibration.crt
      - source: camcalibration-key
        target: certs/scenescape-camcalibration.key
      - django
      - calibration.auth
    healthcheck:
      test: ["CMD", "curl", "-k", "-s", "https://localhost:8443/v1/status"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: always
    pids_limit: 1000

volumes:
  vol-media:
  vol-datasets:
  vol-netvlad_models:
    driver: local
